/**
 * Framework wiring: security, HTTP client, thread pools, typed configuration properties.
 *
 * <p>Kept apart from the adapters so that the composition root is a single, obvious place.
 */
package io.github.iaarencibia.notifications.config;

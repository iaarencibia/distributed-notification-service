# The dispatch timer is off in every context the tests boot. Left on, a poll would fire while a
# test is asserting on rows and the failures would be intermittent. Declared here rather than on
# each test class so a test added later cannot forget it. That the switch defaults to on -- and
# that production therefore gets a timer -- is what DispatchSchedulerConfigTest asserts.
notifications.dispatch.scheduled=false
